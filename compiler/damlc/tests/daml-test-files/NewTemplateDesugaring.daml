daml 1.2

module NewTemplateDesugaring where

-- QUERY-LF .modules[] | .templates[] | select(lf::get_template_name($pkg) == ["T"])

template T
    with
      p : Party
    where
      signatory p
      key p : Party
      maintainer key
