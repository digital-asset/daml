-- This tests that defining a scenario triggers an error when scenarios are disabled.
-- @ERROR Scenarios are no longer supported.
-- Remove when scenarios are fully deleted (TODO - make/find a ticket)
module ScenarioError where

test = scenario do
  pure ()
