canton.participants.participant1.storage {
  type = postgres
  config {
    properties = {
      serverName = "localhost"
      portNumber = "5432"
      databaseName = "canton"
      user = "canton"
      password = "supersafe"
    }
  }
}
canton.participants.participant1.replication.enabled = true
canton.participants.participant2.replication.enabled = false
canton.participants.participant2.storage {
  type = postgres
  config {
    properties = {
      url = "jdbc:postgresql://localhost:5432/canton"
      user = "canton"
      password = "supersafe"
    }
  }
}
