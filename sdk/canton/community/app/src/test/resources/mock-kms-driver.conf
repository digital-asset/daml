# Allow to automatically generate keys with the mock KMS
canton.participants.participant1.init.auto-init = true

canton.participants.participant1.crypto.provider = kms
canton.participants.participant1.crypto.kms {
  type = driver
  name = "mock-kms"
	config = {
      audit-logging = true
    }
}
