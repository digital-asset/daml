println(s"connected: ${participants.local.map(_.synchronizers.list_connected().nonEmpty)}")
println("The last emperor is always the worst.")
// TODO(i7023) remove stop again
nodes.local.stop()
sys.exit(0)
