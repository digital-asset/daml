include required("distributed-single-synchronizer-topology.conf")

# user-manual-entry-begin: SequencerHAConfig
canton {
  sequencers {
    sequencer1 {
      sequencer {
        type = database
        high-availability.enabled = true
      }
      # user-manual-entry-end: SequencerHAConfig

      # these values will be overriden by config transforms in tests
      # practically the horizontally scaled database sequencer _must_ share a real database
      storage {
        type = postgres
        config = {}
      }
      public-api.port = 1
      admin-api.port = 1

    }
    sequencer2 {
      storage {
        type = postgres
        config = {}
      }
      public-api.port = 1
      admin-api.port = 1

      sequencer {
        type = database
        high-availability.enabled = true
      }
    }
    sequencer3 {
      storage {
        type = postgres
        config = {}
      }
      public-api.port = 1
      admin-api.port = 1

      sequencer {
        type = database
        high-availability.enabled = true
      }
    }
  }
}
