echo 'Setting up the DAML development environment...'
eval "$(dev-env/bin/dade assist)"

use nix
watch_file nix/*.nix

# Load private overrides
[[ -f .envrc.private ]] && source_env .envrc.private || :
