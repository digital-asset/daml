
The path command provides access to the tools in the SDK packages.
Here is for example the path to 'damlc':

  $ da path damlc
  /.*/.da/packages/damlc/.*/da-hs-damlc-app (re)

Or we can ask for the path to navigator:

  $ da path navigator
  /.*/.da/packages/navigator/.*/navigator-.*\.jar (re)
