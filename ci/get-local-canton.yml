# Copyright (c) 2023 Digital Asset (Switzerland) GmbH and/or its affiliates. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

steps:
- task: DownloadBuildArtifacts@1
  inputs:
    buildType: current
    downloadType: single
    artifactName: canton_jar
    downloadPath: $(Build.StagingDirectory)/canton_lib
- bash: |
    set -euo pipefail
    if [ -f "$(Build.StagingDirectory)/canton_lib/canton_jar/local-canton.jar" ]; then
      mkdir -p canton/lib
      cp "$(Build.StagingDirectory)/canton_lib/canton_jar/local-canton.jar" canton/lib/
    else
      find "$(Build.StagingDirectory)"
      exit 1
    fi
